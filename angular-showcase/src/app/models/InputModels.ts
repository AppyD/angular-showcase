export interface DropdownChoices {
  value: string;
  label: string;
}

export enum TimeChoices {
  LIVE = "live",
  HISTORICAL = "historical"
}

export enum LocationChoices {
  CURRENT = "current",
  CUSTOM = "custom"
}
